# GitHub Pages 部署配置指南

## 问题说明

GitHub Pages 默认只能托管静态 HTML 文件，而您的项目是 Next.js 应用，需要构建后才能部署。

## 解决方案

已配置 GitHub Actions 自动构建和部署到 GitHub Pages。

---

## ✅ 已完成的配置

### 1. GitHub Actions 工作流
- 文件：`.github/workflows/deploy-pages.yml`
- 功能：自动构建 Next.js 应用并部署到 GitHub Pages

### 2. Next.js 配置更新
- 添加了 `output: 'export'` 用于静态导出
- 配置了 `basePath: '/Stratos'` 用于 GitHub Pages
- 禁用了图片优化（GitHub Pages 不支持）

---

## 📋 部署步骤

### 步骤1：启用 GitHub Pages

1. **访问仓库设置**
   - 打开：https://github.com/charlesnunot/Stratos/settings/pages

2. **配置 Pages 设置**
   - **Source（源）**: 选择 "GitHub Actions"
   - 不要选择 "Deploy from a branch"

3. **保存设置**

### 步骤2：配置环境变量（如果需要）

如果您的应用需要环境变量（如 Supabase 配置）：

1. **访问仓库 Secrets**
   - 打开：https://github.com/charlesnunot/Stratos/settings/secrets/actions

2. **添加 Secrets**
   - 点击 "New repository secret"
   - 添加以下变量（如果需要）：
     - `NEXT_PUBLIC_SUPABASE_URL`
     - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 步骤3：触发部署

有两种方式触发部署：

#### 方式1：自动部署（推荐）
- 每次推送到 `main` 分支时自动部署
- 只需正常提交代码即可

```bash
git add .
git commit -m "更新内容"
git push origin main
```

#### 方式2：手动触发
1. 访问：https://github.com/charlesnunot/Stratos/actions
2. 选择 "Deploy to GitHub Pages" 工作流
3. 点击 "Run workflow"

### 步骤4：等待部署完成

1. **查看部署状态**
   - 访问：https://github.com/charlesnunot/Stratos/actions
   - 查看工作流运行状态

2. **访问网站**
   - 部署成功后，访问：https://charlesnunot.github.io/Stratos/

---

## ⚠️ 重要注意事项

### 1. 静态导出限制

由于使用了 `output: 'export'`，以下功能可能不可用：

- ❌ **API Routes** - Next.js API 路由无法在静态导出中使用
- ❌ **服务器端渲染（SSR）** - 所有页面都是静态生成的
- ❌ **增量静态再生（ISR）** - 不支持
- ✅ **静态生成（SSG）** - 支持
- ✅ **客户端渲染** - 支持

### 2. Supabase 集成

如果您的应用使用 Supabase：

- ✅ **客户端查询** - 可以正常工作
- ✅ **实时订阅** - 可以正常工作
- ❌ **服务器端查询** - 需要使用客户端替代方案

### 3. 路由配置

由于使用了 `basePath: '/Stratos'`：

- 所有链接会自动添加 `/Stratos` 前缀
- 确保内部链接使用相对路径或 `next/link`

---

## 🔧 故障排除

### 问题1：404 错误

**原因**：GitHub Pages 未正确配置或部署未完成

**解决**：
1. 检查 GitHub Pages 设置是否选择了 "GitHub Actions"
2. 查看 Actions 标签页，确认部署是否成功
3. 等待几分钟后刷新页面

### 问题2：资源加载失败

**原因**：路径配置问题

**解决**：
1. 检查 `next.config.js` 中的 `basePath` 配置
2. 确保所有资源使用相对路径
3. 检查浏览器控制台的错误信息

### 问题3：构建失败

**原因**：代码错误或依赖问题

**解决**：
1. 查看 Actions 日志：https://github.com/charlesnunot/Stratos/actions
2. 检查错误信息
3. 在本地运行 `npm run build` 测试构建

### 问题4：环境变量未生效

**原因**：Secrets 未配置

**解决**：
1. 检查 Secrets 配置：https://github.com/charlesnunot/Stratos/settings/secrets/actions
2. 确保变量名以 `NEXT_PUBLIC_` 开头（客户端变量）
3. 重新触发部署

---

## 📝 本地测试

在推送到 GitHub 之前，可以在本地测试构建：

```bash
# 设置环境变量（如果需要）
export NODE_ENV=production

# 构建项目
npm run build

# 查看输出
ls -la out/
```

构建成功后，`out/` 目录包含所有静态文件。

---

## 🚀 部署后

### 访问网站

- **GitHub Pages URL**: https://charlesnunot.github.io/Stratos/

### 更新网站

每次推送到 `main` 分支后，网站会自动更新（通常需要几分钟）。

### 查看部署历史

- 访问：https://github.com/charlesnunot/Stratos/deployments

---

## 🔄 替代方案

如果静态导出无法满足需求，可以考虑：

1. **Vercel** - Next.js 官方推荐，免费且功能完整
2. **Netlify** - 另一个优秀的静态站点托管服务
3. **Railway/Render** - 如果需要服务器端功能

---

## 📚 相关资源

- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Next.js 静态导出](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)
- [GitHub Actions 文档](https://docs.github.com/en/actions)

---

## ✅ 检查清单

- [ ] GitHub Pages 已启用并配置为 "GitHub Actions"
- [ ] 环境变量已配置（如需要）
- [ ] 代码已推送到 `main` 分支
- [ ] GitHub Actions 工作流已成功运行
- [ ] 网站可以正常访问

完成以上步骤后，您的网站应该可以正常访问了！🎉
